---
mode: standard
data_sources: supabase + chrome
degraded_flags: []
escalated: true
trigger: user_explicit_request (no tripwire fired; user asked for comprehensive Chrome scan)
# absolute snapshot (UTC)
total_users: 7
free_users: 6
pro_users: 1
agency_users: 0
subs_active: 0
subs_trialing: 0
total_scans_alltime: 575
scans_this_month: 44
scans_24h: 16
last_scan_at: 2026-05-07T04:07:41Z
total_crawl_batches: 0
# funnel deltas (today UTC)
scan_started_today: 4
scan_completed_today: 4
signup_clicked_today: 0
trial_started_today: 0
checkout_clicked_today: 0
scan_rate_limited_today: 0
email_report_sent_today: 0
---

## 1. Funnel signals (analytics_events)

| window | scan_started | scan_completed | signup_clicked | trial_started | checkout_clicked | email_report_sent | rate_limited |
|---|---|---|---|---|---|---|---|
| today (UTC) | 4 | 4 | 0 | 0 | 0 | 0 | 0 |
| 7d | 50 | 45 | 0 | 0 | 0 | 1 | 0 |
| 28d | 81 | 76 | 0 | 0 | 0 | 1 | 2 |

- **Scan reliability**: 7d = 45/50 = 90.0%, 28d = 76/81 = 93.8%. Healthy.
- **Value-to-account (signup_clicked / scan_completed)**: 0/76 over 28d. **Zero conversions logged** — this is the standout problem. A user just signed up (6→7) but did so without firing `signup_clicked`, suggesting either the event wiring on the signup CTA is broken or the user came in via a non-instrumented path (e.g. direct /login, OAuth provider redirect).
- **Trial/checkout/cancel**: all zero across 28d. Consistent with $0 MRR.
- 14-day scan_logs trend: 0 (4/30) → 1 (5/3) → 4 (5/4) → 16 (5/5) → 16 (5/6) → 4 (5/7 partial). Real increase in usage.

## 2. Reliability signals

- `crawl_batches`: 0 batches all-time. The crawl feature is unused (or scan_logs is the only path).
- `email_events`: 0 rows in last 24h, 1 `email_report_sent` total in 7d/28d. Email delivery layer effectively idle.
- `scan_rate_limited`: 0 in 7d, 2 in 28d (~2.5% of scan_started, well below 20% tripwire).
- No `failed` or `stuck running` crawl batches.

## 3. Traffic & SEO

**GSC (3か月, last refresh 4時間前)**:
- 32 clicks / 327 impressions / CTR 9.8% / avg position 20.9
- Top queries by clicks: `a11yscope` 11/13, then 0-click but visible: `a11y audit` 14imp, `a11y checker` 9imp, `accessibility issues detector` 8imp, `yscope` 7imp, `wcag 2.2` 7imp
- Top pages by clicks: `/` 31/159, `/blog/wcag-2-2-whats-new` 1/120, `/blog` 1/59, `/login` 1/50, `/blog/fix-accessibility-issues-automated-scanners` 0/61, `/faq` 0/48
- Note: `/login` getting 50 imp / 1 click suggests branded "a11yscope login" intent — fine, but no organic upside there.

**Vercel Analytics (Last 7 days)**:
- Visitors 45 (+45% WoW), Page Views 70 (+4%), Bounce Rate 76% (**+18pp worse**)
- Top pages: `/` 37, `/blog` 3, `/blog/how-to-check-website-accessibility-free` 3, `/blog/banking-financial-services-website-accessibility` 2, `/blog/form-accessibility-missing-labels-guide` 2, `/login` 2, `/admin` 1
- Top referrers: **chatgpt.com 13** > google.com 9. ChatGPT is now the #1 traffic source.
- Countries: US 31%, India 24%, Pakistan 11%, Japan 9%, Canada 7%
- Devices: Desktop 84% / Mobile 16%; OS: Windows 67% / Mac 13% / iOS 11% / Android 4%

**Vercel deploy reliability**: latest production = `BgsFxc1Qv` Ready, commit `58af30b` (today's quiet-day report). All 16 visible deploys (Apr 23 → May 7) are Ready, **zero Error** in the last ~14 days.

## 4. Notable activity

- **`www.mediafusion.ai` — 12 anonymous scans from 1 IP in 7d** (score 83–84). Strongest Pro-conversion candidate to date. Recent activity feed confirms 4 of those 12 hit in the last 5 hours alone.
- Other 7d repeat domains: `agendas.townnet.org` (3), `korren-penney.github.io` (3), `uticaparkstg.graydigitaldev.com` (3) — all 1 IP each, lower intent.
- New free user appeared between 5/3 and 5/7 but `signup_clicked = 0` over 28d → **event wiring likely broken on the signup CTA**.

## 5. Actions taken

1. Standard-mode comprehensive scan (Supabase + Chrome MCP across GSC / Admin / Vercel Analytics / Vercel Deployments).
2. Captured 4 production-state screenshots; deploy chain verified Ready end-to-end.
3. No code or pricing changes.

## 6. Recommendations for next run

1. **Wire client beacons** — see investigation §7. Decision needed before next run.
2. **Outreach hypothesis for mediafusion.ai** — 12 scans from one IP in 7d is the strongest commercial signal we've ever seen. Worth a single-message manual outreach decision (out of monitor scope).
3. **Watch ChatGPT referrer trend** — chatgpt.com (13) overtook google.com (9) as #1 referrer this week. If it sustains, consider an LLM-discoverability-oriented blog post instead of more SEO-keyword posts.

## 7. Investigation: client-beacon wiring

**Finding: not broken — never wired.** All three client-fired event kinds have **zero call sites** in the codebase.

Evidence:
- `track("signup_clicked", ...)` — referenced only in the JSDoc example at `src/lib/track.ts:7`. No real call sites.
- `track("checkout_clicked", ...)` — zero call sites.
- `track("email_report_opened", ...)` — zero call sites.
- `import ... from "@/lib/track"` — zero importers (the file is a dead module today).

This perfectly explains the 28d data: server-side events (`scan_started` 81, `scan_completed` 76, `scan_rate_limited` 2, `email_report_sent` 1) all log normally because `lib/analytics.ts:logEvent` is called from API routes. Client funnel kinds are all 0 because no UI fires them.

### Surfaces that should fire `signup_clicked`

| File | Line | What it is |
|---|---|---|
| `src/app/page.tsx` | 184 | Top-nav "Sign In" link → `/login` |
| `src/app/page.tsx` | 308 | Free-scan-limit error CTA "Create a Free Account" → `/login` |
| `src/components/ScanReport.tsx` | 156 | Scan-result card "Create Free Account" → `/login` |
| `src/components/ScanReport.tsx` | 398 | Inline "create a free account" link → `/login` |

All four are bare `<a href="/login">` with no `onClick`. They navigate before any handler gets a chance to fire. (Note: the prominent top-nav "Dashboard" button at `page.tsx:191` also redirects unauthenticated users to `/login` via the dashboard layout — same problem.)

### Surface that should fire `checkout_clicked`

- `src/app/dashboard/billing/CheckoutButton.tsx` — the `handleCheckout` function calls `/api/checkout` directly, no `track()`.
- `src/app/page.tsx:314` — the homepage `handleCheckout("pro")` button on the limit-hit error state, also no `track()`.

### Surface that should fire `email_report_opened`

This kind only makes sense if the email template embeds a 1×1 pixel or tracker URL pointing at a route that calls `track()` server-side. No such route exists. The kind is probably aspirational and can be dropped from the allowlist until an email-tracking strategy is decided.

### Recommended fix (not implemented)

Smallest-surface change:

1. Convert the four `/login` `<a>` tags to a small client component (`<SignupLink from="...">`) that calls `track("signup_clicked", { from })` in `onMouseDown` (fires on left, middle, ctrl-click — survives navigation via `sendBeacon`), then lets the default navigation proceed. **Do not** use `onClick` + `preventDefault` + `window.location` — it breaks middle-click and ctrl-click.
2. Add `track("checkout_clicked", { plan })` at the top of `handleCheckout` in both `CheckoutButton.tsx` and `page.tsx`.
3. Drop `email_report_opened` from `ALLOWED_KINDS` in `src/app/api/events/route.ts` (and the `ClientEventKind` union) until there's a corresponding sender. A truly aspirational kind in the allowlist is just clutter.

Effort: ~1 small PR, <30 lines diff, no schema change. Ships measurement of the most-load-bearing funnel step (`signup_clicked / scan_completed`) which is currently a black hole.

**Decision needed**: implement now, or defer? Recommendation: implement — without it the next 4 weeks of monitor reports keep showing `0 / 76` for signup conversion, which is meaningless. (Out of scope for this scheduled run; flagged for the next manual session.)

### Resolution (this session)

Fix shipped in commit `5241445`. Verified end-to-end on production: the first ever `signup_clicked` row landed in `analytics_events` at 2026-05-07 07:28:51 UTC with `meta: {"from": "home_top_nav"}`. `email_report_opened` removed from the allowlist until a sender exists.

## 8. Comprehensive error sweep

User-requested broad sweep across Sentry, Vercel runtime, Postgres logs, advisors, public pages, browser, and DB integrity. Run after the beacon fix shipped.

### Clean (no action needed)

- **Vercel runtime logs (7d, env=production, level∈error|warning|fatal)**: 0 entries.
- **Postgres logs (24h, all severities)**: only routine `connection authorized` lines. No ERROR or FATAL.
- **Production homepage (`/`)**: 0 console errors, 0 network 4xx/5xx out of 80 requests.
- **Public-page curl sweep** (15 URLs incl. `/`, `/faq`, `/blog`, `/login`, `/admin`, `/tokushoho`, `/privacy`, `/terms`, `/sitemap.xml`, `/robots.txt`, 2 blog posts, 1 use-case, `/api/scan`):
  - 13× 200, plus `/api/scan` 405 (POST-only — correct), `/pricing` 404 (**no internal references** — site uses `#pricing` hash; sitemap does not list `/pricing`; harmless).
- **DB canonical sanity** (7 invariants: orphan FKs, kind/plan/status enums, NULL emails, dup emails): all 0.
- **DB auth-link sanity** (5 invariants on `users.auth_id`): 0 dangling auth_ids, 0 email mismatches, 0 duplicate auth_ids, 0 signed-in auth.users without public.users.

### 🟢 Sentry — only one open issue, and it is NOT a11yscope

`JAVASCRIPT-NEXTJS-3` — `TypeError: Cannot destructure property 'locale' of '(intermediate value)' as it is undefined.` 4 events 16 days ago, 0 users. Stack trace is `src/app/[locale]/not-found.tsx:8`. **Origin URL: `https://experience-villa.com/zh`**. The `vacation-design` Sentry org has a single shared `javascript-nextjs` project, so multiple Vercel projects (a11yscope/accessguard, experience-villa-lp-v2, dashboard, wiki, …) all report into it. Action: route this to the experience-villa repo, not a11yscope. (Side note: consider per-project Sentry projects so issues are unambiguous.)

### 🟡 Supabase advisors — security (3 INFO, 3 WARN)

| Severity | Lint | Detail | Action |
|---|---|---|---|
| INFO | `rls_enabled_no_policy` | `analytics_events`, `crawl_batches`, `email_events` have RLS on but **no policies** — table is locked to anon/authenticated by default; only service role writes. | Likely intentional (write-only telemetry). Confirm intent and document; alternatively add an explicit deny-all policy to silence the lint. |
| WARN | `function_search_path_mutable` | `public.get_scan_counts_by_users`, `public.update_updated_at` — search_path not pinned. | Add `SET search_path = ''` to function definitions ([docs](https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable)). |
| WARN | `auth_leaked_password_protection` | Supabase Auth's HaveIBeenPwned check is disabled. | Toggle on in Supabase Dashboard → Auth → Password Strength. Free, low-risk. |

### 🟡 Supabase advisors — performance (7 WARN, 4 INFO)

| Severity | Lint | Detail | Action |
|---|---|---|---|
| WARN | `auth_rls_initplan` (×7) | Policies on `users`, `sites`, `scan_logs`, `subscriptions` call `auth.uid()` per row. | Replace `auth.uid()` with `(select auth.uid())` in each policy ([docs](https://supabase.com/docs/guides/database/postgres/row-level-security#call-functions-with-select)). Single migration, no behavioural change. Low impact at 7 users / 575 scans, but pre-bake before scaling. |
| INFO | `unused_index` (×4) | `idx_scan_logs_site_id`, `idx_crawl_batches_user_created`, `idx_scan_logs_crawl_batch`, `idx_analytics_events_user_created` never used. | Defer — `crawl_batches` is unused all-time and `analytics_events_user_created` will start being read once the new beacons fill in user_ids. Re-evaluate in 4 weeks. |

### 🟠 Real bug — stale `users.plan` after subscription cancel

**Subject**: `nate@nateatkinson.com` (`public.users.id = 709b643f-…`)
- `users.plan = 'pro'`, **but** all 2 subscription rows have `status = 'canceled'`, latest canceled `2026-03-26` (≈6 weeks ago).
- `users.auth_id IS NULL` — this user has no auth.users row, so they cannot log in (probably created via Stripe webhook before any auth signup completed).
- Net effect: Admin dashboard shows misleading "Pro 1" badge, but they are not paying and cannot use the app. MRR $0 is correct; user count by plan is not.

**Probable cause**: the Stripe webhook handler that processes `customer.subscription.deleted` (or end of `current_period_end`) writes `subscriptions.status = 'canceled'` but does **not** also `UPDATE users SET plan = 'free' WHERE id = $1`.

**Severity**: MEDIUM. Doesn't affect revenue (none anyway), but corrupts internal reporting. Also a latent footgun: if a future Pro user cancels, the same gap will leave them with paid feature access until manually intervened.

**Suggested fix**: add a `UPDATE users SET plan = 'free' ...` in the webhook handler's cancel branch, plus a small migration to backfill the existing case (`UPDATE users SET plan = 'free' WHERE id = '709b643f-3862-4e77-bbfb-6c8cad033a49'`). 1 PR, ~10 lines.

### Ranked next-actions (status post-resolution)

| # | Item | Status |
|---|---|---|
| 1 | Stripe-cancel → `users.plan='free'` webhook fix + backfill | ✅ shipped (`7fd58ca`) + migration `backfill_stale_pro_plan_after_cancel` |
| 2 | `auth_rls_initplan` rewrite of 7 policies | ✅ migration `optimize_rls_policies_initplan` |
| 3a | Function `search_path` pin (2 functions) | ✅ migration `pin_function_search_path` |
| 3b | Enable HaveIBeenPwned password check | ⏳ **manual user action** — Supabase Dashboard → Auth → Password Strength → toggle "Leaked password protection". MCP cannot toggle Auth config. |
| 4 | Telemetry RLS deny-all + table comments | ✅ migration `document_telemetry_table_rls_intent` |
| 5 | Per-project Sentry split | ⏳ deferred (low priority, requires multi-app coordination) |
| 6 | Unused-index re-evaluation | ⏳ scheduled for 2026-06-07 (4 weeks; let new beacons populate `idx_analytics_events_user_created` usage stats first) |

## 9. Resolution summary

Net advisor delta after this session:
- **Security**: 6 lints → **1 lint** (only HaveIBeenPwned remains, which is a Supabase Dashboard toggle outside MCP reach).
- **Performance**: 11 lints → **4 lints** (all remaining are `unused_index` INFO; deferred 4 weeks per plan).

Migrations applied to `hxsiqqyimzeyomesmkeh` in this session:
1. `20260507074532 backfill_stale_pro_plan_after_cancel` — `nate@nateatkinson.com` reset to `plan='free'` (verified post-update).
2. `20260507074559 optimize_rls_policies_initplan` — 7 policies on `users`, `scan_logs`, `subscriptions`, `sites` rewritten with `(select auth.uid())`.
3. `20260507074610 pin_function_search_path` — `update_updated_at()`, `get_scan_counts_by_users(uuid[])` pinned to empty search_path.
4. `20260507074635 document_telemetry_table_rls_intent` — explicit RESTRICTIVE deny-all to anon/authenticated on `analytics_events`, `crawl_batches`, `email_events` + `comment on table` documenting service-role-only design.

Code shipped:
- `5241445` — wire `signup_clicked` × 4 + `checkout_clicked` × 2; drop unwired `email_report_opened` from allowlist.
- `7fd58ca` — `lib/db.ts:updateSubscription` overrides `users.plan` to `'free'` on terminal Stripe statuses (`canceled`, `incomplete_expired`).

Remaining open items (all single-action):
- 🟡 Toggle HaveIBeenPwned in Supabase Dashboard (Auth → Policies → Password strength).
- 🟢 Re-check unused indexes 2026-06-07.
- 🟢 (Long term) split Sentry into per-project projects so the `experience-villa.com/zh` style cross-app noise stays out of a11yscope triage.
