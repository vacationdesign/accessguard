# Weekly Report — 2026-05-11

```
mode: weekly + quick
data_sources: supabase_only (no Chrome this run; weekly does not require it)
degraded_flags: []
escalated: false
trigger: monday_weekly + scheduled (prior weekly = 2026-04-27, 14d gap)
# absolute snapshot (May 11)
total_users: 7
free_users: 7 / pro_users: 0 / agency_users: 0
subs_active: 0 / subs_trialing: 0
total_scans_alltime: 583
scans_this_month: 52
scans_24h: 2
last_scan_at: 2026-05-10 04:10 UTC
total_crawl_batches: 0
```

## 1. Conversion ratios (vs prior weekly 2026-04-27)

The prior weekly used `scan_logs` / `users` proxies because `analytics_events`
was empty. Telemetry is now flowing, so this is the first weekly with the
spec-defined funnel ratios. Treat week-over-week deltas as **not directly
comparable** — different denominators.

| Window | scan_started | scan_completed | signup_clicked | trial_started | checkout_clicked | rate_limited | email_report_sent |
|--------|--------------|----------------|----------------|---------------|------------------|--------------|-------------------|
| 7d  | 57 | 48 | 1 | 0 | 0 | 0 | 1 |
| 28d | 93 | 84 | 1 | 0 | 0 | 2 | 1 |

Derived ratios:

- **scan reliability** (scan_completed / scan_started)
  - 7d: 48/57 = **84.2%**
  - 28d: 84/93 = **90.3%**
- **value-to-account** (signup_clicked / scan_completed)
  - 7d: 1/48 = **2.1%** — denom ≥ 5 ✓
  - 28d: 1/84 = **1.2%** — denom ≥ 5 ✓
- **account-to-trial** (trial_started / signup_clicked)
  - 7d: 0/1 — denom < 5, no signal
  - 28d: 0/1 — denom < 5, no signal
- **end-to-end commercial intent** (checkout_clicked / scan_completed)
  - 7d: 0/48 = **0%**
  - 28d: 0/84 = **0%**

The 7d completion ratio (84%) is meaningfully below the 28d ratio (90%). The
driver is the 2026-05-08 anomaly (see §2). signup_clicked client beacons
were wired on 5/7 (commit `5241445`), so the 1/48 ratio is the first real
read of the value→account funnel — and it is small but non-zero. The 0%
on `checkout_clicked` over 28d is the most concerning number on the page:
nobody who completed a scan has clicked the pricing CTA, period.

## 2. Funnel anomalies (>50% WoW or shape-level)

**2026-05-08 funnel break.** Daily slice: scan_started=5, scan_completed=1
(20% completion). This crosses the daily Tier 1 tripwire (`scan_started > 0
AND scan_completed = 0` does not strictly apply, but a 4/5 abandonment
ratio with rate_limited=0 is consistent with the same failure mode). The
quiet-day report for 5/8 (`3146294`) did not flag this — worth a one-line
post-mortem of the tripwire logic: the current spec only checks for
absolute zero, not for a low completion ratio. Recommend tightening to
`scan_completed / scan_started < 0.5` with denom ≥ 5.

All other days in the 14-day window land at 80-100% completion; 5/8 is
the lone outlier.

## 3. Email deliverability summary (7d)

```
kind          n  unique_recipients
scan_report   2  2
```

No bounces, no complaints, no `onboarding_*` events (only one new signup
in the window, vishvaconnect7@gmail.com on 5/5 — onboarding drip may have
fired and not landed in `email_events`, worth a separate audit if time
permits). No per-recipient cap hits (max 1 report per address). Resend
dashboard not pulled this run.

## 4. Crawl batch health (28d)

Total: **0**. Unchanged from prior weekly. The only Pro user
(nate@nateatkinson.com) was downgraded to free on 5/7 by commit `7fd58ca`
(subscription terminal-state cleanup), so there is now **literally no
account** that can trigger a crawl batch. The pipeline remains exercised
only by manual admin testing.

## 5. Vercel deploy reliability (7d, git-log based)

Not Chrome-verified this run. From `git log`:

- 14 commits between 2026-05-07 and 2026-05-10
- Latest production commit: `65c38a6` (2026-05-10, quiet-day report)
- No `revert` or `rollback` commits
- Several seo-monitor reports + two material code changes:
  - `5241445` — wire signup_clicked + checkout_clicked client beacons
  - `7fd58ca` — downgrade `users.plan` on terminal subscription status

Assume green unless next standard-mode run says otherwise.

## 6. Internal-link audit

Skipped (low-frequency per spec). No new blog posts added since the 4/2
sweep brought all 8 originally-low-linked posts to 4+ incoming links.
Next spot-check: 2026-05-25.

## 7. Repeat-scanner Pro candidates (14d)

| Domain | Scans | Unique IPs | Notes |
|--------|-------|------------|-------|
| www.mediafusion.ai | 17 | 1 | **new heavy user this period** |
| www.jhaveritrade.com | 10 | 2 | trading platform, 2-IP indicates teamwork |
| plantogether.hcp1.net | 7 | 1 | continuing from 4/27 weekly |
| www.grandpaswhitecane.com | 4 | 1 | new |
| uticaparkstg.graydigitaldev.com | 3 | 1 | new (agency staging) |
| buildtogether.hcp1.net | 3 | 1 | hcp1 cluster carries over |
| korren-penney.github.io | 3 | 1 | new |
| agendas.townnet.org | 3 | 1 | new (gov/civic) |

The Apr 27 cohort (jp4.mctx, mctxpct4, imaginesullivant, thelonestar)
has fully aged out of the 14d window — they used the tool intensively
then stopped. The new heavy user **mediafusion.ai (17 scans, 1 IP)** is
the strongest current Pro candidate by far. They are an AI media
company, single user grinding the tool — classic agency-style usage
that the rate-limit copy + signup ladder should convert.

## 8. Operational posture & key state change

**Most important state change since the 4/27 weekly**:
`pro_users: 1 → 0`. The 5/7 `7fd58ca` commit correctly downgraded
nate@nateatkinson.com to `free` after the subscription entered terminal
status. This is a cleanup, not a lost paid user (nate's trial
auto-cancelled long before; the plan field was just stale). **But the
business state is now formally**: 0 paying users, 0 trials, 7 free
accounts, and a working-but-unconverted funnel.

Pipelines:
- Tripwires: **none firing today**.
- Telemetry: **healthy and now exercising** (84% completion in 7d, signup
  beacons wired and producing data).
- Deploy: **assumed healthy** (git-log only this run).
- DB: **healthy** (all queries returned).
- Email: **healthy but quiet** (2 scan_report sends to 2 recipients).
- Tripwire spec gap: **5/8 anomaly was missed** — see §2.

## 9. Top 3 hypotheses for next week

1. **mediafusion.ai is the most convertible repeat user in the funnel
   today.** 17 scans from a single IP over 14d, on a tool that rate-limits
   at 5/hour anonymous. Either they are scanning across multiple sessions
   that never trigger the limit, or they are a heavy single-burst user
   who has not yet hit the wall. Action: confirm they have not already
   created an account under a non-obvious email (e.g., check the IP's
   `user_id IS NOT NULL` scans), then verify the rate-limit dialog is
   surfacing for them. If not surfacing, the new copy never reaches the
   most relevant audience.

2. **checkout_clicked = 0 over 28d is the only flat-zero in the funnel.**
   The pricing CTA is the last narrow gate before money. Hypothesis: the
   CTA is either (a) not surfaced on scan-result pages with high enough
   prominence, or (b) only surfaced to logged-in users — and we have
   2.1% signup conversion, which means very few people see it at all.
   Action next week: inspect where `track('checkout_clicked')` is wired,
   confirm it fires on the public scan-result CTA (not only the
   `/pricing` page), and add it to the rate-limit dialog if missing.

3. **The 5/8 funnel break (5 starts, 1 completion) without a tripwire
   firing is a monitoring bug, not a product bug.** Tighten the tripwire
   spec to include `scan_completed / scan_started < 0.5` (denom ≥ 5) so
   the next degraded day escalates to standard mode automatically.
   Concretely: update `.claude/seo-monitor.md` Step 1.3 table.

## Notes for next run

- Today's UTC slice (2026-05-11) is still empty as of report time (JST 07:00
  ≈ UTC 22:00 the previous day); not an anomaly, just early.
- Resend dashboard / Sentry sweep deferred — pull on the next standard
  escalation rather than burning the weekly window.
