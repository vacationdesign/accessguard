# Weekly Report — 2026-05-25

```
mode: weekly
data_sources: supabase_only
degraded_flags: [chrome_skipped_optional]
escalated: false
trigger: scheduled weekly (Monday, 8d since last weekly 2026-05-17)
# absolute snapshot (UTC; report time 2026-05-24 22:07 UTC = 2026-05-25 07:07 JST)
total_users: 9
free_users: 9 / pro_users: 0 / agency_users: 0
subs_active: 0 / subs_trialing: 0
total_scans_alltime: 715
scans_this_month: 184
scans_24h: 0
last_scan_at: 2026-05-22 13:21:14 UTC
total_crawl_batches: 0
head_commit: bcff5ccb9c1abd82f857d7d72e6a9237a3d19b5c
latest_prod_deploy_commit: not_verified (chrome skipped)
deploy_in_sync: unknown
```

## 1. Conversion ratios (vs prior weekly 2026-05-17)

| Window | scan_started | scan_completed | signup_clicked | trial_started | checkout_clicked | rate_limited | email_report_sent |
|--------|--------------|----------------|----------------|---------------|------------------|--------------|-------------------|
| 7d     | 75  | 70  | 5 | 0 | 0 | 2 | 1 |
| 28d    | 232 | 216 | 6 | 0 | 0 | 4 | 3 |

Derived ratios:

- **scan reliability** (scan_completed / scan_started)
  - 7d: 70/75 = **93.3%**（先週 60/62 = 96.8%、-3.5pp）
  - 28d: 216/232 = **93.1%**（先週 142/153 = 92.8%、+0.3pp）
- **value-to-account** (signup_clicked / scan_completed)
  - 7d: 5/70 = **7.1%**（先週 0/60 = 0%、**+7.1pp**）— denom ≥ 5 ✓
  - 28d: 6/216 = **2.8%**（先週 1/142 = 0.7%、+2.1pp）— denom ≥ 5 ✓
- **account-to-trial** (trial_started / signup_clicked)
  - 7d: 0/5 = **0%** — denom 5（ぎりぎり閾値）
  - 28d: 0/6 = 0% — denom 6
- **end-to-end commercial intent** (checkout_clicked / scan_completed)
  - 7d: 0/70 = 0%（先週同様）
  - 28d: 0/216 = 0%（先週同様）

**今週最大のシグナルは `signup_clicked` が 0 → 5 に復活**したこと。先週の weekly §8 仮説1 で「signup CTA が見えていない／発火していない」と疑った wiring 問題は、5/18-5/22 のスキャンバーストで 5 件発火しているので**ビーコン配線は機能している**ことが確認できた。一方で **signup → trial の transition は依然ゼロ**で、ファネルのボトルネックが「scan_completed → signup_clicked」から「signup_clicked → trial_started」に下流移動した可能性が高い。これが今週の最重要観察。

## 2. Funnel anomalies (>50% WoW or shape-level)

- **signup_clicked: 0 → 5（shape-level、ゼロからの離脱）**。仮説1（§7）の追跡対象を「signup CTA 視認性」から「signup フォーム / 登録完了率」に切り替える必要あり。
- **scan_started: 62 → 75（+21%）/ scan_completed: 60 → 70（+17%）**。WoW 50% 閾値未満だが、5/18 に 24 スキャン / 5/19 に 17 スキャンの 2 日連続バーストが牽引（直近 3 日 5/23-5/25 はゼロスキャン）。
- **3 日連続ゼロスキャン（5/23, 5/24, 5/25）** — 先週の 2 日連続（5/16, 5/17）を上回る最長静止。$0 MRR / 9 ユーザー規模では誤差レベルだが、weekend + Monday 朝なのに完全停止しているのはトラフィック上限（GSC impressions）の動きと突き合わせる価値あり（次回 standard run で確認候補）。
- **新規ユーザー登録 7 → 9（+2）**。先週の weekly では 7、今週 snapshot 9。`users.created_at` 24h クエリは空だったので、登録は 5/18-5/22 の活動期に発生した模様。`signup_clicked` 5 件のうち 2 件が実際の account 作成に到達した（40% 変換）— 小さいが measurable な数字。

## 3. Email deliverability (7d)

```
kind         n  unique_recipients
scan_report  1  1
```

bounce / complaint / per-recipient cap hit いずれもなし。28d ベースでも email_report_sent は 3 件のみで、70 scan_completed に対して **1.4%（7d）/ 1.4%（28d）の利用率**。先週からの動きはなく「機能は壊れていないが誰も使っていない」状態が継続。

email_report_sent は `scan_completed > 30 AND email_report_sent = 0` のトリップワイヤから「= 0」を「= 1」が満たすため発火していないが、**実質的に同じシグナル**（70 完了に対して 1 件は誤差）。週次で連続して "1 件以下" が続けば、トリップワイヤ閾値を `< 0.05 * scan_completed` に書き換える検討を spec 反映候補に。

## 4. Crawl health (28d)

`crawl_batches` テーブルは依然 **0 件**（4 週連続）。`crawl_started` / `crawl_completed` analytics_events も 28d で 0。free ユーザー 9 名で誰一人クロール機能に触れていない。`failed_7d`、`stuck_running` ともに 0。リライアビリティ懸念なしだが、これは「壊れていない」ではなく「動いていない」状態が続いている（spec の "Feature undiscovered" トリップワイヤ拡張候補）。

## 5. Vercel deploy reliability

今回は quick → weekly に直接昇格したため Chrome MCP は起動せず（spec 3.4: "Chrome optional, don't block"）。

- HEAD commit: `bcff5ccb9c1abd82f857d7d72e6a9237a3d19b5c`（5/24 のクワイエットレポートコミット）
- production deploy SHA との一致は今回未確認。次回 standard 昇格時または手動 weekly 再実行時に検証。
- ローカル git log 上では過去 7d に Error 履歴示唆なし、commit は全て chore(seo) の自動コミット。

## 6. GSC snapshot (last 90d)

Chrome MCP 起動なしのため今週は GSC 数値を取得していない。先週（5/17）captured 値:

- Total clicks 38 / impressions 373 / CTR 10.2% / avg position 22.4
- ブランド外クエリの 0-click 問題（`a11y audit` 14 imp、`"shopify store" "accessibility audit"` 13 imp）は前回提案（§8 仮説3）のメタディスクリプション最適化が未実施なら継続している可能性が高い。

3 日連続ゼロスキャンの裏付けに **次回 standard 昇格で GSC impressions が落ちているか**を最優先で確認。

## 7. Internal-link audit

```
4 [全 27 記事の最小値]
```

**全 blog post が 4 以上の内部リンクを保持**（最小 4、5/17 weekly §7 で 0 リンクだった 2 記事 `banking-financial-services-website-accessibility` と `nonprofit-website-accessibility-guide` は backfill 完了で 4 リンクに到達）。spec の "only act if < 3" 条件には誰も該当せず、今週のアクションなし。

## 8. Top 3 hypotheses for next week

1. **signup → trial の transition がゼロな原因を特定する。** 5 件の signup_clicked が trial に進まなかった理由として（a）trial 開始 UI が見つからない、（b）Stripe checkout 画面で離脱、（c）`trial_started` ビーコンが配線されていない、の 3 仮説。`/pricing` から trial 開始までの DOM 動線を手動再現 + `analytics_events` で `trial_started` kind のスキーマ有無を確認するのが最短診断。30 分。
2. **3 日連続ゼロスキャンが GSC impressions の低下と相関しているか確認する。** 次回 standard run 昇格を待たず、weekday の通常時間帯に手動で GSC を確認。impressions が落ちていれば「機能未発見」ではなく「流入そのもの」が課題で、§8 hypothesis 順位が大きく変わる。5 分。
3. **email_report_sent = 1/70（1.4%）の利用率を上げる小さな実験を 1 本打つ。** scan_completed 直後の UI に "結果をメールで受け取る" CTA を inline で配置（モーダルでなく、結果カードの下にボタンとして）。実装 20-30 分、効果は 1 週で測定可能（n=70+ なので統計上ある程度判定できる）。

## 9. Actions taken this run

- Heartbeat SQL 5 本 + 週次 SQL（conversion ratios 7d/28d 現在 vs 前期、email 7d、crawl 28d、scan_logs 14d daily breakdown）
- Internal-link audit 全 27 blog post → 全て 4+ 確認
- Quick + Weekly 両レポート生成、commit + push 予定
- Chrome MCP は spec 3.4 の "optional" 規定に基づき今回スキップ（degraded_flags に明記）

## 10. Notable activity

- 5/18-5/22 のスキャン活動が今週の唯一のトラフィック窓（合計 70 scan）。5/18 の 24 スキャンは過去 14d の単日最大値（前回ピークは 5/14 の 20）。
- 5/23-5/25 の 3 日連続ゼロスキャン × email_report_sent 利用率 1.4% × trial_started ゼロ継続、の 3 要素が「今週は動いた」と「今週も決済まで行かなかった」の両面を要約。
- 新規ユーザー +2（5/17 時点 7 → 5/25 時点 9）は先週 §8 仮説1 のサインアップ wiring 検証アクションに対する間接的な validation。

---

次回 quick run（5/26 火 07:00 JST）で signup_clicked または trial_started のいずれかが動くかが、§8 仮説1 の優先度判定に直結する。
