# Weekly Report — 2026-04-27

```
mode: weekly + quick
data_sources: supabase_only (no Chrome this run; weekly does not require it)
degraded_flags: []
escalated: false
trigger: monday_weekly + scheduled
# absolute snapshot (Apr 27)
total_users: 6
free_users: 5 / pro_users: 1 / agency_users: 0
subs_active: 0 / subs_trialing: 0
total_scans_alltime: 499
scans_this_month: 229
scans_24h: 0
last_scan_at: 2026-04-24 07:47 UTC
total_crawl_batches: 0
analytics_events_alltime: 0
# funnel deltas (today)
all_funnel_kinds_today: 0
new_users_24h: 0
email_events_24h: 0
```

## 1. Conversion ratios

`analytics_events` is empty — telemetry shipped 4/26, no scans since. Use
`scan_logs` and `users` as proxies for the historical window.

| Window | scans | uniq anon IPs | authed scans | new users | new subs |
|--------|-------|---------------|--------------|-----------|----------|
| 7d  | 80  | 14 | 0 | 0 | 0 |
| 28d | 268 | 46 | 14 | 2 | 1 |

Derived ratios (28d, denom small, treat as weak signal):

- **anon → signup**: 2 / 46 = **4.3%**
- **signup → trial**: 1 / 2 = 50% (one signup converted to trial)
- **trial → paid**: 0 / 1 = **0%** (the only trial — andregirao05 — auto-canceled)
- **scan → signup (28d)**: 2 / 268 = **0.75%**

Once telemetry collects a meaningful 7-day window, the proper ratios
(`scan_completed/scan_started`, `signup_clicked/scan_completed`,
`trial_started/signup_clicked`, `checkout_clicked/scan_completed`) will
replace these proxies.

## 2. Funnel anomalies

None observable yet — `analytics_events` total = 0. Only meaningful once
post-deploy traffic produces events.

## 3. Email deliverability summary (7d)

`email_events` 7d: **0 rows.** No `scan_report` requests, no `onboarding_*`
firings (no new free users in 7d). No per-recipient cap hits. No bounces
to investigate (Resend would also need to be checked separately for
deliverability — not done in this run).

## 4. Crawl batch health (28d)

Total: **0**. The crawl pipeline went live with the dashboard UI on 4/25
but no Pro user has run one yet (only Pro user is `nate@nateatkinson.com`
who has never logged in). No failed batches, no stuck `running` batches.

## 5. Vercel deploy reliability (7d)

**20 production deployments, 100% READY.** Latest: `b939767` (chore:
monitoring snapshot + opus launcher). All commits since 4/4 are visible
in the deploy chain — no missing builds, no failed builds.

## 6. Internal-link audit

Skipped re-audit this run. Last full sweep on 4/2 brought all 8
originally-low-linked posts to 4+ incoming links. No drift expected since
no new posts have been added that would change incoming-link counts for
existing posts. Spot-check next weekly.

## 7. Repeat-scanner Pro candidates (14d)

These are the anonymous users who scanned the same domain 3+ times and
have **not converted**:

| Domain | Scans | Unique IPs |
|--------|-------|------------|
| jp4.mctx.org | 27 | 2 |
| www.mctxpct4.org | 19 | 2 |
| imaginesullivant.org | 15 | 2 |
| plantogether.hcp1.net | 12 | 3 |
| www.thelonestar.org | 11 | 1 |
| www.imaginesullivant.org | 7 | 1 |
| www.ecobank.com | 5 | 1 |
| www.thelonestar.org (alt host) | 4 | 1 |
| buildtogether.hcp1.net | 3 | 1 |

These are all textbook Pro candidates. The new "create a free account
for 50 scans/month + history" rate-limit copy + the 3+ scan nudge are
deployed but apparently not converting at this volume. Three of these
(`mctxpct4.org`, `hcp1.net` cluster, `imaginesullivant.org`) are US
local-government / non-profit sites — exactly the cohort the new
nonprofit blog post targets.

## 8. Daily scan trend (14d) — note the dry stretch

| Day | scans | uniq IPs |
|-----|-------|----------|
| 4/27 today | 0 | 0 |
| 4/26 | 0 | 0 |
| 4/25 | 0 | 0 |
| 4/24 | 18 | 4 |
| 4/23 | 1 | 1 |
| 4/22 | 14 | 4 |
| 4/21 | 15 | 4 |
| 4/20 | 32 | 4 |
| 4/19 | (gap)| — |
| 4/18 | 11 | 3 |
| 4/17 | 21 | 2 |
| 4/16 | 3 | 3 |
| 4/15 | 11 | 6 |
| 4/14 | 5 | 1 |
| 4/13 | 5 | 2 |

**Three consecutive zero-scan days (4/25 → 4/27).** Health curls return
200, deploy state is READY across the period, no error spike observed.
This is most likely weekend + post-burst lull (4/24's 18 scans were the
HCP1.net cluster; they have not returned). But if Monday afternoon
JST/EST passes without scans, it's worth confirming the homepage scan
flow is not broken by a recent change. The 4/26 ScanReport restructure
and 4/26 proxy rename both touched the request path.

## 9. Top 3 hypotheses for next week

1. **The HCP1.net / mctxpct4 / imaginesullivan cohort knows the tool
   well and uses it as a free utility.** They have repeatedly hit the
   5/hour limit. The new "free account → 50/month" copy may not be
   landing because the dialog still leads with the Pro $49/month CTA
   visually. Test: add a small banner inside the rate-limit message
   that hides the Pro pitch entirely if the user has triggered the
   limit on the same IP for the second hour in a row, and lead the
   secondary screen with the free-account ladder.

2. **Three zero-scan days following a post-deploy weekend is plausible
   weekend-effect, but the coincidence with the 4/26 ScanReport
   redesign + 4/26 proxy rename is uncomfortable.** Action: tomorrow
   morning, manually run a fresh scan from the homepage and verify the
   `/api/scan` POST returns 200 and a row lands in `scan_logs`. If the
   first traffic-day after this returns zero events, the path is
   probably broken.

3. **Trial conversion is currently 0/1.** The single trial
   (andregirao05) had `cancel_at` scheduled at signup. The new
   onboarding drip + repositioned scan-result CTA + 50/month free tier
   are all post-andregirao05 and have not yet been tested by a real
   trial user. Until at least one new signed-in Free user actually
   exhausts the 50/month — or until at least one new trial completes —
   we cannot judge whether the activation funnel works.

## Operational posture

- Tripwires: **none firing** today.
- Telemetry pipeline: **healthy but unexercised** (0 events because 0 scans).
- Deploy pipeline: **healthy** (100% READY, latest commit live).
- DB pipeline: **healthy** (all queries returned, no errors).
- Email pipeline: **healthy but quiet** (0 events in 7d).
- The biggest risk to the next two weeks is **continuing to ship features
  faster than we can measure them** — every new improvement deployed
  without traffic to test is a guess.
